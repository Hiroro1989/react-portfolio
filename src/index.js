import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//inmport form bootstrap to access additional styling
import 'bootstrap/dist/css/bootstrap.min.css'

ReactDOM.render(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
    document.getElementById('root')
  );